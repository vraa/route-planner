var React = require('react');

var Header = React.createClass({
    render: function () {
        return (
            <header>
                <h1>Route Planner</h1>
            </header>
            );
    }
});

module.exports = Header;